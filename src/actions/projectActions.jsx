import axios from 'axios';
import {GET_ERROR} from './types';
import { async } from 'q';
export const createProject=(project,histry)=async dispatch=>{
    try {
        
    } catch (error) {
        
    }
}